#include <stdio.h>
#include <stdlib.h>

int main() {

  char genero, familia, ordem;
  scanf("%s", &ordem);
  scanf("%s", &familia);
  scanf("%s", &genero);
  if (strcmp(ordem, "vertebrado") == 0) {
    if ()
  } else {
  }

  return 0;
}
